[project]
name = "cgs-set-builders"
version = "0.1.0"
description = "scripts to create the cgs json set fromat"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tcgdex-sdk>=2.3.0",
]
